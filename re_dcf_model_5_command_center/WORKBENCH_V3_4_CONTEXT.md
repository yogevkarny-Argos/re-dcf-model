# Workbench v3.4 Dynamic Dependency Tree

Changes:
- Dependency tree now follows the selected formula trace node.
- Clicking any trace node updates both the drawer and the dependency tree.
- Dependency buttons are generated from the selected trace node's live dependency list.
- No engine math changes.
