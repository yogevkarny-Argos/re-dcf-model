# Workbench v4.1 Contextual UX + Scenario Provenance

Changes:
- Scenario Lab now explains that recipe rows are auto-populated by applying fixed overrides to the current live assumptions and rerunning the engine.
- Added scenario recipe definition table to show source/provenance of each scenario figure.
- Deal Workspace Revenue section now shows a real revenue build summary and unit-type assumption table.
- Sidebar assumption editor is context-aware: it surfaces only assumption groups relevant to the active workspace or active deal section while preserving hidden widget state.
- Kept engine math unchanged.
- Fixed financing section duplicate branch from prior v4 shell.
